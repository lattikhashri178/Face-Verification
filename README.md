# Face Verification System

A web application that verifies if two face images belong to the same person.

## Technologies Used
- Python
- Flask
- face_recognition library
- HTML, CSS, JavaScript

## How to Run
1. Install requirements: pip install flask face_recognition opencv-python
2. Run: python app.py
3. Open browser: http://127.0.0.1:5000
